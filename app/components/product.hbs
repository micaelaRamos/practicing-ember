<LinkTo data-test-product={{@product.id}} @route="item" @model={{@product.id}} class="product">
    <Product::Image @src={{this.productImage}} @itemId={{@product.id}}/>
    <Product::Detail
        @name={{@product.name}}
        @price={{@product.price}}
        @isDetails={{false}}
    />
</LinkTo>